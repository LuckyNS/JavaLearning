public class Car {
    
    int modelYear;
    String modelName;
    
    public Car(int year, String model){ // Creating constructor. Name must be same as class
      modelYear = year;
      modelName = model;
    }
}